
todos = []                              # This will be our input list.

while True:                             # Or can also use 2 > 1 - this will always be True.
    todo = input("Enter an item: \n")   # Users input gets stored into.
    print(todo.capitalize())            # Prints the users input with a capital.
    todos.append(todo)                  # todos is the list, append data ().
